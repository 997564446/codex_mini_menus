# 功能优化需求

后续新增需求时，继续使用当前版本号并递增编号；准备新版本时，再新增一个版本号。需求写完后告诉 Codex：`按照功能优化.md处理`。

## 版本号 1.2.1

-编号：1.2.1-1
-状态：已完成（本地，待发布）
-需求描述：厨师可以修改或删除自己创建的分类；删除分类时不拦截，分类内菜品自动移入系统默认的“未分类”。

处理记录（由 Codex 填写）

-需求理解：普通分类允许改名和删除，“未分类”作为系统默认分类保留且不可删除。
-修改内容：增加分类修改、删除操作；删除时自动迁移关联菜品到“未分类”。
-验证结果：分类接口、家庭隔离和项目结构检查通过。
-未验证项或风险：需要在真实云环境验证删除包含多道菜品的分类。

---

-编号：1.2.1-2
-状态：已完成（当前名称待确认）
-需求描述：将“咱家开饭”改成“今天吃啥捏”。

处理记录（由 Codex 填写）

-需求理解：统一修改小程序名称和页面品牌文案。
-修改内容：当前代码中的名称已统一为“今儿吃啥捏”。
-验证结果：项目配置、入口页、首页和 README 中已不再使用“咱家开饭”。
-未验证项或风险：当前实际文案是“今儿吃啥捏”，与原需求“今天吃啥捏”存在一字差异，需要确认最终名称。

---

-编号：1.2.1-3
-状态：已完成（本地，待发布）
-需求描述：在“未分类”中自动初始化默认菜品，不让厨师手动新增菜品；厨师只需要给菜品分类并设置库存。

处理记录（由 Codex 填写）

-需求理解：菜品由系统预置，首次使用自动进入“未分类”，不提供手动新增入口。
-修改内容：加入 51 道默认菜品初始化逻辑，并限制菜品接口只能修改已有预置菜品。
-验证结果：默认菜品数量、名称唯一性和禁止手动新增的规则检查通过。
-未验证项或风险：需要在新家庭的真实云环境验证首次初始化是否完整且不会重复。

---

-编号：1.2.1-4
-状态：已完成（本地，待发布）
-需求描述：库存支持“有限库存”和“无限库存”；有限库存可以填写数量，库存为 0 时家庭成员不能选择该菜品。

处理记录（由 Codex 填写）

-需求理解：厨师必须能明确选择库存模式，无限库存不扣减，有限库存按实际下单、改单和取消变化。
-修改内容：库存设置页增加有限和无限选项；点餐页禁止选择库存为 0 的菜品；订单流程加入库存扣减与归还。
-验证结果：有限库存、无限库存、库存为 0、改单差额和取消归还的自动测试通过。
-未验证项或风险：需要在真实云数据库并发下验证多人同时抢最后一份库存。

---

-编号：1.2.1-5
-状态：已完成（本地，待发布）
-需求描述：取消餐次菜单，改为周一到周日的星期菜单；任意菜品具体加入星期几，由厨师自行选择，不硬编码 KFC 只能加入周四。

处理记录（由 Codex 填写）

-需求理解：每个家庭固定维护 7 个星期菜单，菜品与星期之间不设置固定限制。
-修改内容：菜单模型和页面改为周一至周日；食客按实际日期读取当天对应的星期菜单。
-验证结果：星期范围和“不硬编码具体菜品星期限制”的自动测试通过。
-未验证项或风险：旧云数据库如仍存在餐次菜单或旧订单唯一索引，需要按迁移文档处理后再发布。

---

-编号：1.2.1-6
-状态：已完成（本地，待发布）
-需求描述：创建分类后，可以在分类中批量勾选菜品；页面采用左右两列，左侧显示分类，右侧显示可归入当前分类的菜品，不需要逐个编辑。

处理记录（由 Codex 填写）

-需求理解：一次勾选并保存整个分类的菜品归属，避免逐道进入详情页操作。
-修改内容：菜品库增加左侧分类导航、右侧多选列表和批量保存；服务端增加带版本校验的批量归类事务。
-验证结果：批量选择校验、接口契约和页面结构检查通过。
-未验证项或风险：需要在真机验证大量菜品勾选、切换分类和版本冲突提示。

---

-编号：1.2.1-7
-状态：已完成（本地，待发布）
-需求描述：菜品库右侧只显示当前分类和“未分类”的菜品；当前分类菜品排在前面，其他已经归类的菜品不能在当前分类中显示。

处理记录（由 Codex 填写）

-需求理解：普通分类只能从“未分类”中接收菜品，不能直接看到或抢走其他分类的菜品。
-修改内容：右侧列表增加分类过滤和排序；选择“未分类”时只显示未分类菜品。
-验证结果：JavaScript、WXML 和项目结构检查通过。
-未验证项或风险：需要在微信开发者工具或真机确认分类切换后的列表和滚动显示。

---

-编号：1.2.1-8
-状态：已完成（本地，待发布）
-需求描述：星期菜单主页继续显示周一到周日；点击某一天的“编辑”进入编辑页后，页面采用左右两列，左侧显示分类，右侧显示当前分类菜品，并提供全选和取消全选功能。

处理记录（由 Codex 填写）

-需求理解：双列布局只放在具体星期的编辑页；全选只影响右侧当前分类，切换分类时保留其他分类已经勾选的菜品。
-修改内容：星期菜单编辑页改为左侧分类、右侧菜品；增加当前分类全选和取消全选，并合并保存全部分类的选择结果。
-验证结果：12 项自动测试全部通过；8 个页面、4 个云函数及 JavaScript、JSON、WXML 结构检查通过。
-未验证项或风险：需要在微信开发者工具和真机验证双列高度、滚动、全选触控及保存后的云端结果。

---

## 版本号 1.2.2

-编号：1.2.2-1
-状态：已完成（云函数已部署，待真机拖动复验）
-需求描述：类别的手动拖动顺序，未分类始终在最下面，只能在未分类中可以手动添加、删除菜品，缩小自定义分类中三个点的点击范围，现在切换分类的时候，很容易就点到，弹出修改名称或删除分类的选项。

处理记录（由 Codex 填写）

-需求理解：自定义分类可拖动排序；“主食”和“未分类”固定在底部；新增和删除菜品只允许在“未分类”操作，预置与自定义菜品都能删除。
-修改内容：增加分类排序模式和服务端顺序保存；缩小“···”响应范围；增加未分类菜品新增、软删除及菜单清理，删除的预置菜品不会再次初始化。2026-08-14 定位到前端已更新但云端 `menu` 仍为旧版，重新绑定 `cloud1` 环境并部署新版 `menu` 后消除“未知菜单接口”报错。
-验证结果：20 项自动测试和项目结构检查通过；云端 `menu` 部署成功，开发者工具模拟器保存分类顺序不再报错。
-未验证项或风险：仍需在真机验证实际拖动顺序能持久保存、滚动冲突和三个点点击范围。

---

-编号：1.2.2-2
-状态：已完成（本地，待部署与真机验证）
-需求描述：星期菜单，点击编辑按钮后，添加一个下拉选项，早餐、中餐、晚餐。选择选项后，设置对应饭点的菜单。

处理记录（由 Codex 填写）

-需求理解：星期菜单主页仍按周一至周日展示，进入某天编辑页后通过下拉框切换三餐并分别保存。
-修改内容：菜单模型升级为 7 天乘 3 餐共 21 份菜单；旧测试菜单自动停用并清空；编辑页增加三餐下拉选择和独立版本控制。
-验证结果：餐别、星期范围、菜单接口和三餐文档标识自动检查通过。
-未验证项或风险：部署前必须建立新索引，并在测试云环境核对旧菜单迁移及 21 份菜单初始化。

---

-编号：1.2.2-3
-状态：已完成（本地，待部署与真机验证）
-需求描述：食客的点餐页面，每天默认出现，早中晚的三餐选项，点进去后，可以订餐。订餐页面也是左侧为分类，右侧是菜品。如果某个类别的菜品，厨师勾选数量是0，则不显示该种类，库存为0的默认显示在最下方，并且灰色禁选。

处理记录（由 Codex 填写）

-需求理解：食客只能查看今天至后天，每天固定显示三餐；每餐独立下单和加菜，分类由当前菜单实际菜品决定。
-修改内容：增加三天点餐范围、三餐入口、双列点餐页、空分类过滤及零库存沉底禁选；订单唯一规则改为每人每天每餐一单；早餐、中餐、晚餐分别在当天 9、14、21 点截止；提前订单在当天 6、10、17 点提醒厨师。
-验证结果：提前天数、三餐截止、每餐订单标识、定时触发器和双列页面结构自动检查通过。
-未验证项或风险：需上传 `order` 定时触发器，并在真实云环境验证北京时间、提醒防重、订阅消息授权额度和库存并发。

---

-编号：1.2.2-4
-状态：已完成（云函数已部署，待食客真机复验）
-需求描述：在未分类上方加一个主食分类，主食也属于默认分类，不可删除，里面加一个默认菜品：不要主食，置顶在最上方，该菜品的数量无限，价格是0。厨师制定每餐的菜单，不要主食这个菜品默认勾选，然后未分类这个类别不显示。食客下单时，主食分类的菜品必选，如果没勾选，不允许下订单。如果勾选不要主食，需要有一个明显的提示，让用户确定，提示语：不吃主食会低血糖的噢。

处理记录（由 Codex 填写）

-需求理解：“主食”和“未分类”都是不可改删的系统分类；菜单不允许使用未分类菜品，每餐至少提供并选择一种主食。
-修改内容：增加“主食”分类和置顶的“不要主食”系统菜品，库存无限、价格为 0；新三餐菜单默认勾选该菜品；菜单编辑隐藏未分类；食客选择“不要主食”时弹出指定提示；客户端和服务端同时校验主食必选。2026-08-14 部署新版 `menu` 与 `order` 云函数，使已有家庭自动补建系统分类和菜品，并启用服务端主食必选校验。
-验证结果：20 项自动测试和项目结构检查通过；云端 `menu`、`order` 部署成功，开发者工具模拟器已出现“主食（1）”。
-未验证项或风险：仍需用食客真机验证菜单默认勾选“不要主食”、主食必选拦截及低血糖确认提示。

-编号：1.2.2-5
-状态：已完成（本地，待部署与真机验证）
-需求描述：重新初始化一下菜品，另外厨师手动添加菜品时，菜品、库存、单价必填。

处理记录（由 Codex 填写）

-需求理解：按 `初始化菜品.txt` 的最新清单重新初始化默认菜品，包括菜名、单价和有限/无限库存；厨师新增菜品时必须明确填写名称、库存和单价。
-修改内容：默认菜品更新为 71 道带价格和库存的结构化数据；增加 `1.2.2-5` 一次性初始化版本，已有家庭首次加载时重置预置菜品并停用旧清单残留菜品，后续不会反复覆盖厨师设置；新增菜品页面增加单价输入，有限库存数量不再默认代填；客户端和云函数同时校验库存与整数分单价。
-验证结果：22 项自动测试全部通过；8 个页面、4 个云函数及 JSON、JavaScript、WXML 结构检查通过；代码中的 71 道默认菜品与 `初始化菜品.txt` 逐项一致。
-未验证项或风险：需部署新版 `menu` 云函数并在测试云环境打开一次菜品库，核对 71 道菜品、价格、库存和旧预置菜品停用结果；重新初始化会把现有预置菜品恢复到“未分类”并按新清单重置价格与库存，自定义菜品不受影响。

-编号：1.2.2-6
-状态：已完成（本地，待部署与真机验证）
-需求描述：解除每餐60道的限制，没有意义。

处理记录（由 Codex 填写）

-需求理解：厨师设置单餐菜单时不应有 60 道上限；食客查看和提交订单的完整链路也不能在 60 道处截断或拒绝。
-修改内容：删除菜单保存的 60 道限制，取消菜品批量读取时只取前 60 道的截断，并解除订单条目最多 60 种菜品的限制；菜品去重、有效性、库存、主食必选和单道数量校验保持不变。
-验证结果：23 项自动测试全部通过；8 个页面、4 个云函数及 JSON、JavaScript、WXML 结构检查通过；新增 61 种菜品订单通过测试，并确认代码中不再存在菜单 60 道截断或拒绝逻辑。
-未验证项或风险：需部署新版 `menu` 和 `order` 云函数，并在真实云环境验证超过 60 道菜单的保存、读取和下单；仍受微信云函数请求体、数据库文档大小等平台容量限制。

---

## 后续新增需求

复制下面的内容，递增编号后填写：

-编号：1.2.2-7
-状态：待处理
-需求描述：

处理记录（由 Codex 填写）

-需求理解：
-修改内容：
-验证结果：
-未验证项或风险：
