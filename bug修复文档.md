# Bug 修复记录

本文件持续记录已经解决的问题。每条记录包含问题表现、出现原因、解决方案、验证结果和剩余风险。

## BUG-20260814-01：分类排序报“未知菜单接口”，主食分类未出现

- 关联需求：`1.2.2-1`、`1.2.2-4`
- 状态：已解决并部署
- 问题表现：保存分类拖动顺序时报“未知菜单接口”；菜品库没有“主食”和“不要主食”。
- 出现原因：小程序前端已经更新到 `1.2.2`，但微信开发者工具没有为 `cloudfunctions` 根目录绑定云环境，导致新版 `menu`、`order` 云函数没有成功上传，手机仍在调用旧版云函数。
- 解决方案：为云函数根目录重新绑定 `cloud1`，上传并部署新版 `menu` 和 `order`；重新进入菜品库时自动补建系统“主食”分类和“不要主食”菜品。
- 验证结果：云端 `menu`、`order` 更新时间已刷新；开发者工具模拟器保存分类顺序不再报错，并显示“主食（1）”。
- 剩余风险：仍需在食客真机验证主食必选和“不吃主食会低血糖的噢。”确认提示。

## BUG-20260814-02：批量调整分类时误移动其他菜品

- 关联需求：`1.2.2-1`、`1.2.2-4`
- 状态：已解决并部署
- 问题表现：先把包子、饺子、米饭从“家常菜”取消，再勾选到“主食”时，番茄鸡蛋、火锅等没有操作的菜品也被划入“主食”。
- 出现原因：页面原来提交“当前分类最终完整勾选列表”，服务端据此推断移入和移出。这种协议把客户端复选框列表当成完整事实；只要分类切换或长列表渲染后出现状态漏项，服务端就会把未出现在列表中的菜品误判为用户明确取消，造成无关菜品被迁移。
- 解决方案：批量归类接口改为增量协议，只提交 `addDishIds` 和 `removeDishIds`；页面逐道记录用户明确勾选或取消的菜品；服务端只移动这些菜品，并校验它们当前必须位于“未分类”或目标分类，其他菜品一律保持原分类。
- 验证结果：`npm test` 的 21 项测试全部通过，`npm run check` 通过；新版 `menu` 云函数已部署，小程序 `1.2.2` 已重新上传体验版。使用新版接口修复现有数据后，模拟器显示“家常菜 16、主食 4、未分类 0”，其中主食仅保留系统“不要主食”和包子、饺子、米饭。
- 剩余风险：开发者工具模拟器已复验通过；仍需在手机体验版按原操作路径再复验一次微信真机的交互表现。
